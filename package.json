{
  "name": "team-seokan",
  "version": "2.0.0",
  "description": "짱구 캐릭터 기반 멀티 에이전트 오케스트레이션 시스템 for Claude Code",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "dev": "bun run --watch src/index.ts",
    "test": "bun test",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "install-plugin": "bun run build && cp -r dist ~/.claude/plugins/team-seokan/"
  },
  "keywords": [
    "claude-code",
    "plugin",
    "multi-agent",
    "orchestration",
    "team-seokan"
  ],
  "author": "seokan",
  "license": "MIT",
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
